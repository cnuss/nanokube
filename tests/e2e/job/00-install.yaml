apiVersion: batch/v1
kind: Job
metadata:
  name: job-test
spec:
  template:
    spec:
      containers:
      - name: busybox
        image: busybox:latest
        command: ["echo", "hello from job"]
      restartPolicy: Never
  backoffLimit: 2
