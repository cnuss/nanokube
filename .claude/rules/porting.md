# Porting Code

When converting, porting, or moving existing code to a new location:

1. Copy every line verbatim first
2. Only remove lines the user explicitly identifies as unnecessary
3. If something looks removable, ask — do not decide
4. Understand why each line exists before touching it
