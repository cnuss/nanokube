diff --git a/cmd/kubelet/app/server.go b/cmd/kubelet/app/server.go
index c245a648831..741125215f6 100644
--- a/cmd/kubelet/app/server.go
+++ b/cmd/kubelet/app/server.go
@@ -1260,7 +1260,7 @@ func RunKubelet(ctx context.Context, kubeServer *options.KubeletServer, kubeDeps
 		kubeDeps.OSInterface = kubecontainer.RealOS{}
 	}
 
-	k, err := createAndInitKubelet(ctx,
+	k, err := CreateAndInitKubelet(ctx,
 		kubeServer,
 		kubeDeps,
 		hostname,
@@ -1303,7 +1303,7 @@ func startKubelet(ctx context.Context, k kubelet.Bootstrap, podCfg *config.PodCo
 	go k.ListenAndServePods(ctx)
 }
 
-func createAndInitKubelet(
+func CreateAndInitKubelet(
 	ctx context.Context,
 	kubeServer *options.KubeletServer,
 	kubeDeps *kubelet.Dependencies,
diff --git a/pkg/controlplane/controller/kubernetesservice/controller.go b/pkg/controlplane/controller/kubernetesservice/controller.go
index d8cfd44222c..b2b1c9e71a3 100644
--- a/pkg/controlplane/controller/kubernetesservice/controller.go
+++ b/pkg/controlplane/controller/kubernetesservice/controller.go
@@ -85,6 +85,12 @@ func New(config Config, client kubernetes.Interface, serviceInformer v1informers
 // Start begins the core controller loops that must exist for bootstrapping
 // a cluster.
 func (c *Controller) Start(stopCh <-chan struct{}) {
+	// nanokube: skip bootstrap of the default "kubernetes" Service and its
+	// endpoint reconciliation. The controller is left constructed so Stop()
+	// can be called safely, but no work is performed.
+	klog.Info("nanokube: kubernetesservice.Controller.Start is a no-op")
+	return
+
 	if !cache.WaitForCacheSync(stopCh, c.serviceSynced) {
 		runtime.HandleError(fmt.Errorf("timed out waiting for caches to sync"))
 		return
@@ -111,6 +117,10 @@ func (c *Controller) Start(stopCh <-chan struct{}) {
 
 // Stop cleans up this API Servers endpoint reconciliation leases so another master can take over more quickly.
 func (c *Controller) Stop() {
+	// nanokube: matched no-op for Start; nothing was started, nothing to stop.
+	klog.Info("nanokube: kubernetesservice.Controller.Stop is a no-op")
+	return
+
 	c.lock.Lock()
 	defer c.lock.Unlock()
 
diff --git a/pkg/kubelet/client/kubelet_client.go b/pkg/kubelet/client/kubelet_client.go
index b9d366ad462..045e062d751 100644
--- a/pkg/kubelet/client/kubelet_client.go
+++ b/pkg/kubelet/client/kubelet_client.go
@@ -19,6 +19,7 @@ package client
 import (
 	"context"
 	"fmt"
+	"net"
 	"net/http"
 	"strconv"
 	"time"
@@ -198,6 +199,18 @@ func (k *NodeConnectionInfoGetter) GetConnectionInfo(ctx context.Context, nodeNa
 		return nil, err
 	}
 
+	// nanokube: if the selected address already carries a port (e.g. "fqdn:443"
+	// from an external address), honor it and ignore DaemonEndpoints.
+	if h, p, splitErr := net.SplitHostPort(host); splitErr == nil {
+		return &ConnectionInfo{
+			Scheme:                         k.scheme,
+			Hostname:                       h,
+			Port:                           p,
+			Transport:                      k.transport,
+			InsecureSkipTLSVerifyTransport: k.insecureSkipTLSVerifyTransport,
+		}, nil
+	}
+
 	// Use the kubelet-reported port, if present
 	port := int(node.Status.DaemonEndpoints.KubeletEndpoint.Port)
 	if port <= 0 {
diff --git a/pkg/kubelet/kubelet.go b/pkg/kubelet/kubelet.go
index 5bf378bab58..ca625ed3357 100644
--- a/pkg/kubelet/kubelet.go
+++ b/pkg/kubelet/kubelet.go
@@ -65,6 +65,7 @@ import (
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/apiserver/pkg/server/dynamiccertificates"
 	"k8s.io/apiserver/pkg/server/flagz"
+	"k8s.io/apiserver/pkg/server/healthz"
 	utilfeature "k8s.io/apiserver/pkg/util/feature"
 	coreinformersv1 "k8s.io/client-go/informers/core/v1"
 	clientset "k8s.io/client-go/kubernetes"
@@ -572,7 +573,9 @@ func NewMainKubelet(ctx context.Context,
 				return nil, err
 			}
 		} else {
-			return nil, err
+			// nanokube: oomWatcher creation fails on macOS (no /proc); ignore.
+			logger.V(2).Info("Failed to create an oomWatcher (ignoring)", "err", err)
+			oomWatcher = nil
 		}
 	}
 
@@ -3300,6 +3303,20 @@ func (kl *Kubelet) ListenAndServe(ctx context.Context, kubeCfg *kubeletconfigint
 	server.ListenAndServeKubeletServer(ctx, kl, kl.resourceAnalyzer, kl.containerManager.GetHealthCheckers(), kl.flagz, kubeCfg, tlsConfig, auth, tp)
 }
 
+// nanokube: accessors to enable building a *kubeletserver.Server outside the
+// kubelet package (kubeletserver.NewServer needs these private fields).
+
+// ResourceAnalyzer returns the kubelet's resource analyzer.
+func (kl *Kubelet) ResourceAnalyzer() serverstats.ResourceAnalyzer { return kl.resourceAnalyzer }
+
+// HealthCheckers returns the kubelet's container manager health checkers.
+func (kl *Kubelet) HealthCheckers() []healthz.HealthChecker {
+	return kl.containerManager.GetHealthCheckers()
+}
+
+// Flagz returns the kubelet's flagz reader.
+func (kl *Kubelet) Flagz() flagz.Reader { return kl.flagz }
+
 // ListenAndServeReadOnly runs the kubelet HTTP server in read-only mode.
 func (kl *Kubelet) ListenAndServeReadOnly(ctx context.Context, address net.IP, port uint, tp trace.TracerProvider) {
 	server.ListenAndServeKubeletReadOnlyServer(ctx, kl, kl.resourceAnalyzer, kl.containerManager.GetHealthCheckers(), kl.flagz, address, port, tp)
diff --git a/pkg/kubelet/server/server.go b/pkg/kubelet/server/server.go
index fe96a4ab366..190486668ad 100644
--- a/pkg/kubelet/server/server.go
+++ b/pkg/kubelet/server/server.go
@@ -469,14 +469,17 @@ func (s *Server) getMetricMethodBucket(method string) string {
 // to this set. New handlers should be added under InstallAuthorizedHandlers.
 func (s *Server) InstallAuthNotRequiredHandlers(ctx context.Context) {
 	logger := klog.FromContext(ctx)
-	s.addMetricsBucketMatcher("healthz")
-	checkers := []healthz.HealthChecker{
-		healthz.PingHealthz,
-		healthz.LogHealthz,
-		healthz.NamedCheck("syncloop", s.host.SyncLoopHealthCheck),
-	}
-	checkers = append(checkers, s.extendedCheckers...)
-	healthz.InstallHandler(s.restfulCont, checkers...)
+	// nanokube: skip kubelet's /healthz so the apiserver chain mounted on
+	// our listener can own it instead. Kubelet doesn't register /livez or
+	// /readyz, only /healthz.
+	// s.addMetricsBucketMatcher("healthz")
+	// checkers := []healthz.HealthChecker{
+	// 	healthz.PingHealthz,
+	// 	healthz.LogHealthz,
+	// 	healthz.NamedCheck("syncloop", s.host.SyncLoopHealthCheck),
+	// }
+	// checkers = append(checkers, s.extendedCheckers...)
+	// healthz.InstallHandler(s.restfulCont, checkers...)
 
 	slis.SLIMetricsWithReset{}.Install(s.restfulCont)
 
@@ -878,6 +881,10 @@ func (s *Server) getContainerLogs(request *restful.Request, response *restful.Re
 	}
 
 	response.Header().Set("Transfer-Encoding", "chunked")
+	// nanokube: return 206 Partial Content to bypass Cloudflare edge's
+	// 128KB chunked-response buffer. apiserver proxies kubelet's response
+	// body through unchanged, so the 206 propagates end-to-end.
+	response.WriteHeader(http.StatusPartialContent)
 	if err := s.host.GetKubeletContainerLogs(ctx, kubecontainer.GetPodFullName(pod), containerName, logOptions, stdout, stderr); err != nil {
 		response.WriteError(http.StatusBadRequest, err)
 		return
@@ -1278,6 +1285,12 @@ func (s *Server) ServeHTTP(w http.ResponseWriter, req *http.Request) {
 	handler.ServeHTTP(w, req)
 }
 
+// Restful returns the kubelet's REST container. Embedders that built a
+// *Server outside this package use it to install additional WebServices
+// (Add) or raw http.Handlers (Handle) on the kubelet's listener. The
+// container's filter chain still applies.
+func (s *Server) Restful() containerInterface { return s.restfulCont }
+
 // prometheusHostAdapter adapts the HostInterface to the interface expected by the
 // cAdvisor prometheus collector.
 type prometheusHostAdapter struct {
diff --git a/pkg/registry/core/pod/rest/subresources.go b/pkg/registry/core/pod/rest/subresources.go
index f5160574bb4..bb3a1c661b4 100644
--- a/pkg/registry/core/pod/rest/subresources.go
+++ b/pkg/registry/core/pod/rest/subresources.go
@@ -309,6 +309,10 @@ func (r *PortForwardREST) Connect(ctx context.Context, name string, opts runtime
 func newThrottledUpgradeAwareProxyHandler(location *url.URL, transport http.RoundTripper, wrapTransport, upgradeRequired bool, responder rest.Responder) http.Handler {
 	handler := proxy.NewUpgradeAwareHandler(location, transport, wrapTransport, upgradeRequired, proxy.NewErrorResponder(responder))
 	handler.MaxBytesPerSec = capabilities.Get().PerConnectionBandwidthLimitBytesPerSec
+	// nanokube: rewrite outgoing Host header to match the backend URL. Without
+	// this, the incoming Host (apiserver's cloudflare FQDN) is forwarded and
+	// cloudflare routes the apiserver→kubelet subrequest back to the apiserver.
+	handler.UseLocationHost = true
 	return handler
 }
 
diff --git a/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/get.go b/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/get.go
index 134e163eeb3..fb0fa2008a6 100644
--- a/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/get.go
+++ b/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/get.go
@@ -275,6 +275,12 @@ func handleWatch(ctx context.Context, rw rest.Watcher, scope *RequestScope, req
 	if timeout == 0 && minRequestTimeout > 0 {
 		timeout = time.Duration(float64(minRequestTimeout) * (rand.Float64() + 1.0))
 	}
+	// nanokube: cap watch timeouts so the apiserver closes the response
+	// frequently — this forces Cloudflare's edge to flush its buffered
+	// body. See docs/cloudflare-sse-limits.md.
+	if timeout > 5*time.Second {
+		timeout = 5 * time.Second
+	}
 
 	klog.V(3).InfoS("Starting watch", "path", req.URL.Path, "resourceVersion", opts.ResourceVersion, "labels", opts.LabelSelector, "fields", opts.FieldSelector, "sendInitialEvents", opts.SendInitialEvents, "timeout", timeout, "audit-ID", audit.GetAuditIDTruncated(ctx))
 	ctx, cancel := context.WithTimeout(ctx, timeout)
diff --git a/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/responsewriters/writers.go b/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/responsewriters/writers.go
index cb782f76a3e..b6eb6722bc9 100644
--- a/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/responsewriters/writers.go
+++ b/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/responsewriters/writers.go
@@ -39,6 +39,7 @@ import (
 	"k8s.io/apiserver/pkg/endpoints/handlers/negotiation"
 	"k8s.io/apiserver/pkg/endpoints/metrics"
 	"k8s.io/apiserver/pkg/endpoints/request"
+	genericrest "k8s.io/apiserver/pkg/registry/generic/rest"
 	"k8s.io/apiserver/pkg/registry/rest"
 	utilfeature "k8s.io/apiserver/pkg/util/feature"
 	"k8s.io/apiserver/pkg/util/flushwriter"
@@ -73,6 +74,12 @@ func StreamObject(statusCode int, gv schema.GroupVersion, s runtime.NegotiatedSe
 	if len(contentType) == 0 {
 		contentType = "application/octet-stream"
 	}
+	// nanokube: forward the backend response's status code when the
+	// streamer recorded one (e.g. pods/log passes kubelet's 206 through so
+	// cloudflare's edge skips its 128KB chunked-response buffer).
+	if ls, ok := stream.(*genericrest.LocationStreamer); ok && ls.StatusCode != 0 {
+		statusCode = ls.StatusCode
+	}
 	w.Header().Set("Content-Type", contentType)
 	w.WriteHeader(statusCode)
 	// Flush headers, if possible
diff --git a/staging/src/k8s.io/apiserver/pkg/registry/generic/rest/streamer.go b/staging/src/k8s.io/apiserver/pkg/registry/generic/rest/streamer.go
index b1a61991bf5..c2b49b9f32d 100644
--- a/staging/src/k8s.io/apiserver/pkg/registry/generic/rest/streamer.go
+++ b/staging/src/k8s.io/apiserver/pkg/registry/generic/rest/streamer.go
@@ -43,6 +43,12 @@ type LocationStreamer struct {
 	Flush           bool
 	ResponseChecker HttpResponseChecker
 	RedirectChecker func(req *http.Request, via []*http.Request) error
+	// nanokube: InputStream records the backend's HTTP status code here so
+	// StreamObject can forward it to the client instead of the caller's
+	// default 200. Used by pods/log so kubelet's 206 Partial Content (which
+	// bypasses Cloudflare's 128KB chunked-response buffer) propagates
+	// end-to-end.
+	StatusCode int
 	// TLSVerificationErrorCounter is an optional value that will Inc every time a TLS error is encountered.  This can
 	// be wired a single prometheus counter instance to get counts overall.
 	TLSVerificationErrorCounter CounterMetric
@@ -104,6 +110,10 @@ func (s *LocationStreamer) InputStream(ctx context.Context, apiVersion, acceptHe
 		}
 	}
 
+	// nanokube: capture backend status so StreamObject can forward it
+	// end-to-end.
+	s.StatusCode = resp.StatusCode
+
 	contentType = s.ContentType
 	if len(contentType) == 0 {
 		contentType = resp.Header.Get("Content-Type")
diff --git a/staging/src/k8s.io/cri-streaming/pkg/streaming/remotecommand/websocket.go b/staging/src/k8s.io/cri-streaming/pkg/streaming/remotecommand/websocket.go
index 3bbeeddb3e6..01c9400203c 100644
--- a/staging/src/k8s.io/cri-streaming/pkg/streaming/remotecommand/websocket.go
+++ b/staging/src/k8s.io/cri-streaming/pkg/streaming/remotecommand/websocket.go
@@ -37,6 +37,11 @@ const (
 	preV4Base64WebsocketProtocol = wsstream.Base64ChannelWebSocketProtocol
 	v4BinaryWebsocketProtocol    = "v4." + wsstream.ChannelWebSocketProtocol
 	v4Base64WebsocketProtocol    = "v4." + wsstream.Base64ChannelWebSocketProtocol
+	// nanokube: v5 is v4 with an additional stream-close signal (see
+	// wsstream doc.go); binary-only. The handshake/handle code in
+	// wsstream.Conn already recognizes this protocol via
+	// protocolSupportsStreamClose.
+	v5BinaryWebsocketProtocol = "v5." + wsstream.ChannelWebSocketProtocol
 )
 
 // createChannels returns the standard channel types for a shell connection (STDIN 0, STDOUT 1, STDERR 2)
@@ -93,6 +98,10 @@ func createWebSocketStreams(req *http.Request, w http.ResponseWriter, opts *Opti
 			Binary:   false,
 			Channels: channels,
 		},
+		v5BinaryWebsocketProtocol: {
+			Binary:   true,
+			Channels: channels,
+		},
 	})
 	conn.SetIdleTimeout(idleTimeout)
 	negotiatedProtocol, streams, err := conn.Open(httpresponse.GetOriginal(w), req)
@@ -122,7 +131,7 @@ func createWebSocketStreams(req *http.Request, w http.ResponseWriter, opts *Opti
 	}
 
 	switch negotiatedProtocol {
-	case v4BinaryWebsocketProtocol, v4Base64WebsocketProtocol:
+	case v4BinaryWebsocketProtocol, v4Base64WebsocketProtocol, v5BinaryWebsocketProtocol:
 		ctx.writeStatus = v4WriteStatusFunc(streams[errorChannel])
 	default:
 		ctx.writeStatus = v1WriteStatusFunc(streams[errorChannel])
