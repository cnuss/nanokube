diff --git a/pkg/controlplane/controller/kubernetesservice/controller.go b/pkg/controlplane/controller/kubernetesservice/controller.go
index d8cfd44222c..b2b1c9e71a3 100644
--- a/pkg/controlplane/controller/kubernetesservice/controller.go
+++ b/pkg/controlplane/controller/kubernetesservice/controller.go
@@ -85,6 +85,12 @@ func New(config Config, client kubernetes.Interface, serviceInformer v1informers
 // Start begins the core controller loops that must exist for bootstrapping
 // a cluster.
 func (c *Controller) Start(stopCh <-chan struct{}) {
+	// nanokube: skip bootstrap of the default "kubernetes" Service and its
+	// endpoint reconciliation. The controller is left constructed so Stop()
+	// can be called safely, but no work is performed.
+	klog.Info("nanokube: kubernetesservice.Controller.Start is a no-op")
+	return
+
 	if !cache.WaitForCacheSync(stopCh, c.serviceSynced) {
 		runtime.HandleError(fmt.Errorf("timed out waiting for caches to sync"))
 		return
@@ -111,6 +117,10 @@ func (c *Controller) Start(stopCh <-chan struct{}) {
 
 // Stop cleans up this API Servers endpoint reconciliation leases so another master can take over more quickly.
 func (c *Controller) Stop() {
+	// nanokube: matched no-op for Start; nothing was started, nothing to stop.
+	klog.Info("nanokube: kubernetesservice.Controller.Stop is a no-op")
+	return
+
 	c.lock.Lock()
 	defer c.lock.Unlock()
 
diff --git a/pkg/kubelet/client/kubelet_client.go b/pkg/kubelet/client/kubelet_client.go
index 00d08e6f800..db15b0c3951 100644
--- a/pkg/kubelet/client/kubelet_client.go
+++ b/pkg/kubelet/client/kubelet_client.go
@@ -19,6 +19,7 @@ package client
 import (
 	"context"
 	"fmt"
+	"net"
 	"net/http"
 	"strconv"
 	"time"
@@ -197,6 +198,18 @@ func (k *NodeConnectionInfoGetter) GetConnectionInfo(ctx context.Context, nodeNa
 		return nil, err
 	}
 
+	// If the selected address already carries a port (e.g. "fqdn:443" from an
+	// external address), honor it and ignore DaemonEndpoints.
+	if h, p, splitErr := net.SplitHostPort(host); splitErr == nil {
+		return &ConnectionInfo{
+			Scheme:                         k.scheme,
+			Hostname:                       h,
+			Port:                           p,
+			Transport:                      k.transport,
+			InsecureSkipTLSVerifyTransport: k.insecureSkipTLSVerifyTransport,
+		}, nil
+	}
+
 	// Use the kubelet-reported port, if present
 	port := int(node.Status.DaemonEndpoints.KubeletEndpoint.Port)
 	if port <= 0 {
diff --git a/pkg/kubelet/kubelet.go b/pkg/kubelet/kubelet.go
index b8b7cfd84e9..8e2880b2801 100644
--- a/pkg/kubelet/kubelet.go
+++ b/pkg/kubelet/kubelet.go
@@ -33,6 +33,7 @@ import (
 	"sync/atomic"
 	"time"
 
+	"github.com/emicklei/go-restful/v3"
 	cadvisorapi "github.com/google/cadvisor/info/v1"
 	inuserns "github.com/moby/sys/userns"
 	"github.com/opencontainers/selinux/go-selinux"
@@ -335,6 +336,7 @@ type Dependencies struct {
 	PodStartupLatencyTracker  util.PodStartupLatencyTracker
 	NodeStartupLatencyTracker util.NodeStartupLatencyTracker
 	HealthChecker             watchdog.HealthChecker
+	Services                  []*restful.WebService
 	// remove it after cadvisor.UsingLegacyCadvisorStats dropped.
 	useLegacyCadvisorStats bool
 }
@@ -569,7 +571,8 @@ func NewMainKubelet(ctx context.Context,
 				return nil, err
 			}
 		} else {
-			return nil, err
+			klog.V(2).InfoS("Failed to create an oomWatcher (ignoring)", "err", err)
+			oomWatcher = nil
 		}
 	}
 
@@ -702,6 +705,7 @@ func NewMainKubelet(ctx context.Context,
 	)
 
 	klet.resourceAnalyzer = serverstats.NewResourceAnalyzer(ctx, klet, kubeCfg.VolumeStatsAggPeriod.Duration, kubeDeps.Recorder)
+	klet.additionalServices = kubeDeps.Services
 
 	klet.runtimeService = kubeDeps.RemoteRuntimeService
 
@@ -1517,6 +1521,13 @@ type Kubelet struct {
 
 	// flagz is the Reader interface to get flags for flagz page.
 	flagz flagz.Reader
+
+	// nanokube patch: additional WebServices registered on the kubelet's
+	// HTTP server alongside the built-in routes. Populated from
+	// Dependencies.Services in NewMainKubelet and consumed by
+	// ListenAndServe so callers (e.g. per-backend streaming endpoints)
+	// inherit the container's auth/tracing/metrics filters.
+	additionalServices []*restful.WebService
 }
 
 // ListPodStats is delegated to StatsProvider, which implements stats.Provider interface
@@ -3168,6 +3179,12 @@ func (kl *Kubelet) ListenAndServe(ctx context.Context, kubeCfg *kubeletconfigint
 	server.ListenAndServeKubeletServer(ctx, kl, kl.resourceAnalyzer, kl.containerManager.GetHealthCheckers(), kl.flagz, kubeCfg, tlsOptions, auth, tp)
 }
 
+// AdditionalServices returns extra restful WebServices to register on the
+// kubelet HTTP server (populated from Dependencies.Services).
+func (kl *Kubelet) AdditionalServices() []*restful.WebService {
+	return kl.additionalServices
+}
+
 // ListenAndServeReadOnly runs the kubelet HTTP server in read-only mode.
 func (kl *Kubelet) ListenAndServeReadOnly(ctx context.Context, address net.IP, port uint, tp trace.TracerProvider) {
 	server.ListenAndServeKubeletReadOnlyServer(ctx, kl, kl.resourceAnalyzer, kl.containerManager.GetHealthCheckers(), kl.flagz, address, port, tp)
diff --git a/pkg/kubelet/server/server.go b/pkg/kubelet/server/server.go
index 31ecfe65672..05ec4e5e7fa 100644
--- a/pkg/kubelet/server/server.go
+++ b/pkg/kubelet/server/server.go
@@ -182,6 +182,12 @@ func ListenAndServeKubeletServer(
 	logger.Info("Starting to listen", "address", address, "port", port)
 	handler := NewServer(ctx, host, resourceAnalyzer, checkers, flagz, auth, kubeCfg)
 	handler.InstallTracingFilter(tp)
+	// nanokube patch: register additional WebServices (e.g. per-backend
+	// streaming endpoints) on the kubelet's restful container so they
+	// inherit the container's auth/tracing/metrics filters.
+	for _, ws := range host.AdditionalServices() {
+		handler.restfulCont.Add(ws)
+	}
 
 	s := &http.Server{
 		Addr:           net.JoinHostPort(address.String(), strconv.FormatUint(uint64(port), 10)),
@@ -286,6 +292,7 @@ type HostInterface interface {
 	GetPortForward(ctx context.Context, podName, podNamespace string, podUID types.UID, portForwardOpts portforward.V4Options) (*url.URL, error)
 	ListMetricDescriptors(ctx context.Context) ([]*runtimeapi.MetricDescriptor, error)
 	ListPodSandboxMetrics(ctx context.Context) ([]*runtimeapi.PodSandboxMetrics, error)
+	AdditionalServices() []*restful.WebService
 }
 
 // NewServer initializes and configures a kubelet.Server object to handle HTTP requests.
@@ -835,6 +842,10 @@ func (s *Server) getContainerLogs(request *restful.Request, response *restful.Re
 	}
 
 	response.Header().Set("Transfer-Encoding", "chunked")
+	// nanokube patch: return 206 Partial Content to bypass Cloudflare edge's
+	// 128KB chunked-response buffer. apiserver proxies kubelet's response
+	// body through unchanged, so the 206 propagates end-to-end.
+	response.WriteHeader(http.StatusPartialContent)
 	if err := s.host.GetKubeletContainerLogs(ctx, kubecontainer.GetPodFullName(pod), containerName, logOptions, stdout, stderr); err != nil {
 		response.WriteError(http.StatusBadRequest, err)
 		return
diff --git a/pkg/kubelet/server/server_test.go b/pkg/kubelet/server/server_test.go
index 97091722ef5..f87e44b7545 100644
--- a/pkg/kubelet/server/server_test.go
+++ b/pkg/kubelet/server/server_test.go
@@ -36,6 +36,7 @@ import (
 
 	cadvisorapi "github.com/google/cadvisor/info/v1"
 	cadvisorapiv2 "github.com/google/cadvisor/info/v2"
+	restful "github.com/emicklei/go-restful/v3"
 	"github.com/stretchr/testify/assert"
 	"github.com/stretchr/testify/require"
 	"k8s.io/utils/ptr"
@@ -152,6 +153,10 @@ func (fk *fakeKubelet) ListPodSandboxMetrics(ctx context.Context) ([]*runtimeapi
 	return nil, nil
 }
 
+func (fk *fakeKubelet) AdditionalServices() []*restful.WebService {
+	return nil
+}
+
 func (fk *fakeKubelet) SyncLoopHealthCheck(req *http.Request) error {
 	duration := fk.resyncInterval * 2
 	minDuration := time.Minute * 5
diff --git a/pkg/registry/core/pod/rest/subresources.go b/pkg/registry/core/pod/rest/subresources.go
index e6ffa3408ff..446b9cebfc5 100644
--- a/pkg/registry/core/pod/rest/subresources.go
+++ b/pkg/registry/core/pod/rest/subresources.go
@@ -281,5 +281,9 @@ func (r *PortForwardREST) Connect(ctx context.Context, name string, opts runtime
 func newThrottledUpgradeAwareProxyHandler(location *url.URL, transport http.RoundTripper, wrapTransport, upgradeRequired bool, responder rest.Responder) http.Handler {
 	handler := proxy.NewUpgradeAwareHandler(location, transport, wrapTransport, upgradeRequired, proxy.NewErrorResponder(responder))
 	handler.MaxBytesPerSec = capabilities.Get().PerConnectionBandwidthLimitBytesPerSec
+	// nanokube: rewrite outgoing Host header to match the backend URL. Without
+	// this, the incoming Host (apiserver's cloudflare FQDN) is forwarded and
+	// cloudflare routes the apiserver→kubelet subrequest back to the apiserver.
+	handler.UseLocationHost = true
 	return handler
 }
diff --git a/pkg/volume/util/atomic_writer.go b/pkg/volume/util/atomic_writer.go
index 0466dffacdf..9e26ad0a9b4 100644
--- a/pkg/volume/util/atomic_writer.go
+++ b/pkg/volume/util/atomic_writer.go
@@ -446,8 +446,8 @@ func (w *AtomicWriter) writePayloadToDir(payload map[string]FileProjection, dir
 		}
 
 		if err := w.chown(fullPath, int(*fileProjection.FsUser), -1); err != nil {
-			klog.Errorf("%s: unable to change file %s with owner %v: %v", w.logContext, fullPath, int(*fileProjection.FsUser), err)
-			return err
+			klog.Warningf("%s: unable to change file %s with owner %v: %v", w.logContext, fullPath, int(*fileProjection.FsUser), err)
+			return nil
 		}
 	}
 
diff --git a/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/get.go b/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/get.go
index eb52baf1946..f4bb6038681 100644
--- a/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/get.go
+++ b/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/get.go
@@ -259,6 +259,10 @@ func ListResource(r rest.Lister, rw rest.Watcher, scope *RequestScope, forceWatc
 			if timeout == 0 && minRequestTimeout > 0 {
 				timeout = time.Duration(float64(minRequestTimeout) * (rand.Float64() + 1.0))
 			}
+			// nanokube: prevent long timeouts
+			if timeout > 10*time.Second {
+				timeout = 10 * time.Second
+			}
 
 			klog.V(3).InfoS("Starting watch", "path", req.URL.Path, "resourceVersion", opts.ResourceVersion, "labels", opts.LabelSelector, "fields", opts.FieldSelector, "timeout", timeout)
 			ctx, cancel := context.WithTimeout(ctx, timeout)
diff --git a/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/responsewriters/writers.go b/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/responsewriters/writers.go
index 55f8b657a3c..4b7a01cfe5c 100644
--- a/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/responsewriters/writers.go
+++ b/staging/src/k8s.io/apiserver/pkg/endpoints/handlers/responsewriters/writers.go
@@ -40,6 +40,7 @@ import (
 	"k8s.io/apiserver/pkg/endpoints/handlers/negotiation"
 	"k8s.io/apiserver/pkg/endpoints/metrics"
 	"k8s.io/apiserver/pkg/endpoints/request"
+	genericrest "k8s.io/apiserver/pkg/registry/generic/rest"
 	"k8s.io/apiserver/pkg/registry/rest"
 	utilfeature "k8s.io/apiserver/pkg/util/feature"
 	"k8s.io/apiserver/pkg/util/flushwriter"
@@ -73,6 +74,12 @@ func StreamObject(statusCode int, gv schema.GroupVersion, s runtime.NegotiatedSe
 	if len(contentType) == 0 {
 		contentType = "application/octet-stream"
 	}
+	// nanokube patch: forward the backend response's status code when the
+	// streamer recorded one (e.g. pods/log passes kubelet's 206 through so
+	// cloudflare's edge skips its 128KB chunked-response buffer).
+	if ls, ok := stream.(*genericrest.LocationStreamer); ok && ls.BackendStatus != 0 {
+		statusCode = ls.BackendStatus
+	}
 	w.Header().Set("Content-Type", contentType)
 	w.WriteHeader(statusCode)
 	// Flush headers, if possible
diff --git a/staging/src/k8s.io/apiserver/pkg/registry/generic/rest/streamer.go b/staging/src/k8s.io/apiserver/pkg/registry/generic/rest/streamer.go
index b1a61991bf5..82d7a0426d7 100644
--- a/staging/src/k8s.io/apiserver/pkg/registry/generic/rest/streamer.go
+++ b/staging/src/k8s.io/apiserver/pkg/registry/generic/rest/streamer.go
@@ -43,6 +43,12 @@ type LocationStreamer struct {
 	Flush           bool
 	ResponseChecker HttpResponseChecker
 	RedirectChecker func(req *http.Request, via []*http.Request) error
+	// nanokube patch: InputStream records the backend's HTTP status code
+	// here so StreamObject can forward it to the client instead of the
+	// caller's default 200. Used by pods/log so kubelet's 206 Partial
+	// Content (which bypasses Cloudflare's 128KB chunked-response buffer)
+	// propagates end-to-end.
+	BackendStatus int
 	// TLSVerificationErrorCounter is an optional value that will Inc every time a TLS error is encountered.  This can
 	// be wired a single prometheus counter instance to get counts overall.
 	TLSVerificationErrorCounter CounterMetric
@@ -104,6 +110,10 @@ func (s *LocationStreamer) InputStream(ctx context.Context, apiVersion, acceptHe
 		}
 	}
 
+	// nanokube patch: always capture backend status so StreamObject can
+	// forward it end-to-end.
+	s.BackendStatus = resp.StatusCode
+
 	contentType = s.ContentType
 	if len(contentType) == 0 {
 		contentType = resp.Header.Get("Content-Type")
diff --git a/staging/src/k8s.io/kubelet/pkg/cri/streaming/remotecommand/websocket.go b/staging/src/k8s.io/kubelet/pkg/cri/streaming/remotecommand/websocket.go
index 25900888522..263d78bbbc7 100644
--- a/staging/src/k8s.io/kubelet/pkg/cri/streaming/remotecommand/websocket.go
+++ b/staging/src/k8s.io/kubelet/pkg/cri/streaming/remotecommand/websocket.go
@@ -37,6 +37,10 @@ const (
 	preV4Base64WebsocketProtocol = wsstream.Base64ChannelWebSocketProtocol
 	v4BinaryWebsocketProtocol    = "v4." + wsstream.ChannelWebSocketProtocol
 	v4Base64WebsocketProtocol    = "v4." + wsstream.Base64ChannelWebSocketProtocol
+	// v5 is v4 with an additional stream-close signal (see wsstream doc.go);
+	// binary-only. Handshake/handle code in wsstream.Conn already recognizes
+	// this protocol via protocolSupportsStreamClose.
+	v5BinaryWebsocketProtocol = "v5." + wsstream.ChannelWebSocketProtocol
 )
 
 // createChannels returns the standard channel types for a shell connection (STDIN 0, STDOUT 1, STDERR 2)
@@ -93,6 +97,10 @@ func createWebSocketStreams(req *http.Request, w http.ResponseWriter, opts *Opti
 			Binary:   false,
 			Channels: channels,
 		},
+		v5BinaryWebsocketProtocol: {
+			Binary:   true,
+			Channels: channels,
+		},
 	})
 	conn.SetIdleTimeout(idleTimeout)
 	negotiatedProtocol, streams, err := conn.Open(responsewriter.GetOriginal(w), req)
@@ -122,7 +130,7 @@ func createWebSocketStreams(req *http.Request, w http.ResponseWriter, opts *Opti
 	}
 
 	switch negotiatedProtocol {
-	case v4BinaryWebsocketProtocol, v4Base64WebsocketProtocol:
+	case v4BinaryWebsocketProtocol, v4Base64WebsocketProtocol, v5BinaryWebsocketProtocol:
 		ctx.writeStatus = v4WriteStatusFunc(streams[errorChannel])
 	default:
 		ctx.writeStatus = v1WriteStatusFunc(streams[errorChannel])
diff --git a/staging/src/k8s.io/kubelet/pkg/cri/streaming/server.go b/staging/src/k8s.io/kubelet/pkg/cri/streaming/server.go
index fe5c22b0497..0a76aa19419 100644
--- a/staging/src/k8s.io/kubelet/pkg/cri/streaming/server.go
+++ b/staging/src/k8s.io/kubelet/pkg/cri/streaming/server.go
@@ -58,6 +58,8 @@ type Server interface {
 	Start(stayUp bool) error
 	// Stop the server, and terminate any open connections.
 	Stop() error
+
+	WebService() *restful.WebService
 }
 
 // Runtime is the interface to execute the commands and provide the streams.
@@ -145,6 +147,7 @@ func NewServer(config Config, runtime Runtime) (Server, error) {
 	handler := restful.NewContainer()
 	handler.Add(ws)
 	s.handler = handler
+	s.ws = ws
 	s.server = &http.Server{
 		Addr:      s.config.Addr,
 		Handler:   s.handler,
@@ -160,6 +163,11 @@ type server struct {
 	handler http.Handler
 	cache   *requestCache
 	server  *http.Server
+	ws      *restful.WebService
+}
+
+func (s *server) WebService() *restful.WebService {
+	return s.ws
 }
 
 func validateExecRequest(req *runtimeapi.ExecRequest) error {
