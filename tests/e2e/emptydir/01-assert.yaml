apiVersion: kuttl.dev/v1beta1
kind: TestAssert
commands:
  - script: kubectl exec --namespace $NAMESPACE emptydir-test -- touch /mnt/default/.probe
  - script: kubectl exec --namespace $NAMESPACE emptydir-test -- touch /mnt/memory/.probe
